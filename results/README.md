# Resultados locais

Organizo as tabelas em `tables/`, os relatórios em `reports/` e os gráficos em `figures/`. Apresento as métricas do estudo em `docs/execucao.md` e mantenho os arquivos gerados fora do Git.
